# Схема работы двойной аналитики

## 🔄 Поток данных событий

```
┌─────────────────────────────────────────────────────────────┐
│                   ПОЛЬЗОВАТЕЛЬСКОЕ ДЕЙСТВИЕ                  │
│         (клик, отправка формы, выбор города и т.д.)         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              КОМПОНЕНТ React (например, Button)              │
│                   onClick={() => track...()}                 │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│           📦 src/services/analytics.ts (ЕДИНАЯ ТОЧКА)        │
│                                                               │
│   export function trackApplicationSent(type, amount) {       │
│     YM.trackApplicationSent(type, amount);  // Яндекс        │
│     GA.gaTrackApplicationSent(type, amount); // Google       │
│   }                                                           │
└───────────────────┬───────────────────┬─────────────────────┘
                    │                   │
       ┌────────────┘                   └────────────┐
       │                                             │
       ▼                                             ▼
┌──────────────────────┐                  ┌──────────────────────┐
│  Яндекс.Метрика      │                  │  Google Analytics 4  │
│  metrika-goals.ts    │                  │  ga4-events.ts       │
│                      │                  │                      │
│  window.ym(          │                  │  window.gtag(        │
│    105974763,        │                  │    'event',          │
│    'reachGoal',      │                  │    'event_name',     │
│    goal,             │                  │    params            │
│    params            │                  │  )                   │
│  )                   │                  │                      │
└──────────┬───────────┘                  └──────────┬───────────┘
           │                                         │
           ▼                                         ▼
┌──────────────────────┐                  ┌──────────────────────┐
│   mc.yandex.ru       │                  │  google-analytics.   │
│   (Метрика API)      │                  │  com/g/collect       │
│                      │                  │  (GA4 API)           │
│  ID: 105974763       │                  │  ID: G-PL79VVNQWD    │
└──────────┬───────────┘                  └──────────┬───────────┘
           │                                         │
           ▼                                         ▼
┌──────────────────────┐                  ┌──────────────────────┐
│  Панель Яндекс.      │                  │  Панель Google       │
│  Метрики             │                  │  Analytics 4         │
│                      │                  │                      │
│  metrika.yandex.ru   │                  │  analytics.google.   │
│  /dashboard          │                  │  com                 │
└──────────────────────┘                  └──────────────────────┘
```

---

## 📊 Модульная структура

```
src/services/
│
├── analytics.ts               ⭐ ГЛАВНЫЙ МОДУЛЬ (используйте этот!)
│   │
│   ├── trackApplicationSent() ─┬─→ YM.trackApplicationSent()
│   │                            └─→ GA.gaTrackApplicationSent()
│   │
│   ├── trackPhoneClick() ──────┬─→ YM.trackPhoneClick()
│   │                            └─→ GA.gaTrackPhoneClick()
│   │
│   ├── trackCitySelected() ────┬─→ YM.trackCitySelected()
│   │                            └─→ GA.gaTrackCitySelected()
│   │
│   └── ... (все 12 функций) ───┴─→ Дублируются в обе системы
│
├── metrika-goals.ts           🔵 Только Яндекс.Метрика
│   │
│   ├── METRIKA_ID = 105974763
│   ├── MetrikaGoals (константы)
│   ├── reachGoal(goal, params)
│   └── trackApplicationSent(), trackPhoneClick(), ...
│
└── ga4-events.ts              🟢 Только Google Analytics 4
    │
    ├── GA4Events (константы)
    ├── sendEvent(name, params)
    └── gaTrackApplicationSent(), gaTrackPhoneClick(), ...
```

---

## 🎯 Как использовать

### ✅ Правильно (рекомендуется)

```typescript
// Импортируйте из analytics.ts
import { trackApplicationSent } from '@/services/analytics';

function handleSubmit() {
  // Событие уйдет СРАЗУ в обе системы
  trackApplicationSent('family', 5000000);
}
```

**Результат**: 
- ✅ Яндекс.Метрика получит событие
- ✅ Google Analytics 4 получит событие
- ✅ В консоли 2 лога:
  ```
  Metrika goal: application_sent { program: "family", amount: 5000000 }
  GA4 event: application_sent { program_type: "family", value: 5000000, currency: "RUB" }
  ```

---

### ❌ Неправильно (устарело)

```typescript
// НЕ импортируйте напрямую из metrika-goals или ga4-events
import { trackApplicationSent } from '@/services/metrika-goals'; // ❌

function handleSubmit() {
  // Событие уйдет ТОЛЬКО в Яндекс.Метрику, GA4 не получит!
  trackApplicationSent('family', 5000000);
}
```

---

## 📈 Карта событий

| Действие пользователя | Функция трекинга | Яндекс.Метрика | Google Analytics 4 |
|------------------------|------------------|----------------|-------------------|
| Отправка заявки | `trackApplicationSent()` | ✅ `application_sent` | ✅ `application_sent` |
| Клик по телефону | `trackPhoneClick()` | ✅ `phone_click` | ✅ `phone_click` |
| Клик Telegram | `trackTelegramClick()` | ✅ `telegram_click` | ✅ `telegram_click` |
| Использование калькулятора | `trackCalculatorUsed()` | ✅ `calculator_used` | ✅ `calculator_used` |
| Завершение квиза | `trackQuizCompleted()` | ✅ `quiz_completed` | ✅ `quiz_completed` |
| Просмотр программы | `trackProgramViewed()` | ✅ `program_viewed` | ✅ `program_viewed` |
| Переключение вкладок | `trackTabChanged()` | ✅ `tab_changed` | ✅ `tab_changed` |
| Выбор города | `trackCitySelected()` | ✅ `city_selected` | ✅ `city_selected` |
| Отправка формы | `trackFormSubmitted()` | ✅ `form_submitted` | ✅ `form_submitted` |
| Поиск города | `trackSearchUsed()` | ✅ `search_used` | ✅ `search_used` |
| Смена фильтра | `trackFilterChanged()` | ✅ `filter_changed` | ✅ `filter_changed` |
| Взаимодействие с картой | `trackMapInteraction()` | ✅ `map_interaction` | ✅ `map_interaction` |

---

## 🔍 Проверка работы

### В консоли браузера (F12)

При каждом событии должны появляться **2 лога**:

```javascript
// 1. Яндекс.Метрика
Metrika goal: city_selected { city: "Симферополь" }

// 2. Google Analytics 4
GA4 event: city_selected { city_name: "Симферополь" }
```

Если видите **только 1 лог** — что-то не работает!

---

### В панелях аналитики

#### Яндекс.Метрика (realtime)
```
1. Откройте https://metrika.yandex.ru/dashboard?id=105974763
2. Кликните на событие на сайте
3. В разделе "Конверсии → Цели" увидите +1 к счетчику
```

#### Google Analytics 4 (realtime)
```
1. Откройте https://analytics.google.com
2. Reports → Realtime → Events
3. Кликните на событие на сайте
4. Через 1-5 секунд увидите событие в списке
```

---

## 🛠 Добавление нового события

### Шаг 1: Добавьте в `metrika-goals.ts`

```typescript
export const MetrikaGoals = {
  // ...
  NEW_EVENT: 'new_event',
} as const;

export function trackNewEvent(param: string) {
  reachGoal(MetrikaGoals.NEW_EVENT, { param });
}
```

### Шаг 2: Добавьте в `ga4-events.ts`

```typescript
export const GA4Events = {
  // ...
  NEW_EVENT: 'new_event',
} as const;

export function gaTrackNewEvent(param: string) {
  sendEvent(GA4Events.NEW_EVENT, { param_name: param });
}
```

### Шаг 3: Объедините в `analytics.ts`

```typescript
export function trackNewEvent(param: string) {
  YM.trackNewEvent(param);
  GA.gaTrackNewEvent(param);
}
```

### Шаг 4: Используйте в компоненте

```typescript
import { trackNewEvent } from '@/services/analytics';

function MyComponent() {
  return (
    <button onClick={() => trackNewEvent('test')}>
      Click me
    </button>
  );
}
```

---

## 📝 Чеклист миграции (если используете старый код)

Если в вашем проекте есть прямые импорты из `metrika-goals.ts`:

- [ ] Найти все `import { ... } from '@/services/metrika-goals'`
- [ ] Заменить на `import { ... } from '@/services/analytics'`
- [ ] Проверить в консоли — должны быть 2 лога (YM + GA4)
- [ ] Проверить в панелях — события приходят в обе системы
- [ ] Удалить неиспользуемые импорты

---

## 🎓 Полезные материалы

### Документация проекта
- [ANALYTICS.md](./ANALYTICS.md) - Полная инструкция по аналитике
- [PUBLIC_API.md](./PUBLIC_API.md) - API функций трекинга
- [GA4_SETUP.md](./GA4_SETUP.md) - Настройка Google Analytics 4

### Панели доступа
- [Яндекс.Метрика](https://metrika.yandex.ru/dashboard?id=105974763)
- [Google Analytics 4](https://analytics.google.com)

---

**Статус системы**: ✅ Работает  
**Версия схемы**: 1.0  
**Дата**: 28 декабря 2024
