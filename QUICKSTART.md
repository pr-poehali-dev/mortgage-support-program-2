# 🚀 Быстрый старт

Это краткое руководство поможет вам быстро начать работу с проектом "Ипотека Крым".

## ⚡ За 5 минут

### 1. Клонируйте репозиторий
```bash
git clone <repository-url>
cd mortgage-support-program-2
```

### 2. Установите зависимости
```bash
npm install
# или
bun install
```

### 3. Запустите dev сервер
```bash
npm run dev
# или
bun dev
```

### 4. Откройте в браузере
```
http://localhost:5173
```

✅ Готово! Сайт работает локально.

---

## 📊 Проверка аналитики

### Откройте консоль браузера (F12)

При взаимодействии с сайтом вы увидите логи:

```javascript
// Пример логов в консоли
Metrika goal: city_selected { city: "Симферополь" }
Metrika goal: form_submitted { city: "Ялта", source: "region_map" }
Metrika goal: filter_changed { filter: "city", results: 11 }
```

### Яндекс.Метрика

Откройте панель метрики:
```
https://metrika.yandex.ru/dashboard?id=105974763
```

Проверьте раздел **"Конверсии" → "Цели"**

---

## 🎯 Основные функции

### 1. Трекинг событий

```typescript
import { trackCitySelected, trackPhoneClick } from '@/services/metrika-goals';

// В обработчике события
function handleClick() {
  trackCitySelected('Симферополь');
}
```

### 2. Отправка заявки

```typescript
const response = await fetch(
  'https://functions.poehali.dev/927c8f65-0024-4ded-8d22-24987e241c4e',
  {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      name: 'Иван',
      phone: '+79781234567',
      city: 'Ялта'
    })
  }
);
```

### 3. Использование компонентов

```tsx
import RegionsMapSection from '@/components/home/RegionsMapSection';

export default function MyPage() {
  return (
    <div>
      <h1>Моя страница</h1>
      <RegionsMapSection />
    </div>
  );
}
```

---

## 📁 Структура проекта (кратко)

```
src/
├── components/home/          # Компоненты главной
│   ├── RegionsMapSection.tsx # Карта Крыма (ГЛАВНЫЙ)
│   └── regions/              # Подкомпоненты карты
│       ├── CityFilterAndSearch.tsx
│       ├── CrimeaMapSVG.tsx
│       ├── DistrictsGrid.tsx
│       └── ApplicationForm.tsx
├── services/
│   └── metrika-goals.ts      # Аналитика (ВАЖНО)
└── data/
    └── crimea-cities.ts      # 40+ городов
```

---

## 🔧 Частые задачи

### Добавить новое событие аналитики

1. Откройте `src/services/metrika-goals.ts`
2. Добавьте в `MetrikaGoals`:
```typescript
NEW_EVENT: 'new_event',
```
3. Создайте функцию:
```typescript
export function trackNewEvent(param: string) {
  reachGoal(MetrikaGoals.NEW_EVENT, { param });
}
```
4. Используйте в компоненте:
```typescript
import { trackNewEvent } from '@/services/metrika-goals';
trackNewEvent('value');
```

### Изменить список городов

1. Откройте `src/data/crimea-cities.ts`
2. Добавьте/измените город:
```typescript
export const crimeaCities = [
  // ...
  { 
    name: 'Новый город', 
    x: 400,    // координата X на карте
    y: 250,    // координата Y на карте
    size: 'medium',  // large | medium | small
    type: 'city'     // city | town
  },
];
```

### Обновить backend функцию

1. Внесите изменения в `backend/telegram-notification/index.py`
2. Используйте `sync_backend` tool в poehali.dev
3. Проверьте деплой

---

## 🧪 Тестирование

### Проверка формы заявки

1. Откройте сайт
2. Нажмите на любой город на карте
3. Заполните форму (имя, телефон)
4. Нажмите "Отправить заявку"
5. Проверьте:
   - ✅ Toast уведомление "Заявка отправлена"
   - ✅ Форма закрылась
   - ✅ Событие в консоли
   - ✅ Уведомление в Telegram

### Проверка поиска

1. Введите в поиск "Симф"
2. Увидите выпадающий список с "Симферополь"
3. Кликните на результат
4. Откроется форма с этим городом

### Проверка фильтров

1. Нажмите "Города"
2. На карте останутся только города (11 шт)
3. Нажмите "ПГТ"
4. На карте останутся только ПГТ (30 шт)

---

## 📊 Доступ к метрике

### Вход в Яндекс.Метрику

1. Зайдите на https://metrika.yandex.ru
2. Войдите в аккаунт Яндекс
3. Выберите счетчик **105974763**
4. Откройте раздел нужных отчетов

### Основные отчеты

- **Посещаемость**: Стандартные отчеты → Сводка
- **Цели**: Стандартные отчеты → Конверсии → Цели
- **География**: Стандартные отчеты → Посетители → География
- **Вебвизор**: Вебвизор → Записи визитов
- **Карта кликов**: Карта кликов → Главная страница

---

## 🔗 Полезные ссылки

### Документация проекта
- [PROJECT.md](./PROJECT.md) - Полная документация
- [ANALYTICS.md](./ANALYTICS.md) - Руководство по аналитике
- [PUBLIC_API.md](./PUBLIC_API.md) - API для разработчиков
- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура системы

### Внешние ресурсы
- [Яндекс.Метрика](https://metrika.yandex.ru)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [React Docs](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com)

### Проект
- **Production**: https://ипотекакрым.рф
- **Platform**: https://poehali.dev
- **Functions**: https://functions.poehali.dev

---

## 🆘 Проблемы?

### Сайт не запускается

```bash
# Очистите кеш и переустановите
rm -rf node_modules
rm package-lock.json
npm install
npm run dev
```

### Метрика не работает

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что скрипт метрики загружен:
```javascript
console.log(window.ym); // должна быть функция
```
3. Проверьте ID счетчика (105974763)

### Backend не отвечает

1. Проверьте URL функции
2. Проверьте CORS (должен быть '*')
3. Проверьте секреты (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
4. Посмотрите логи в poehali.dev

### Форма не отправляется

1. Откройте консоль (F12)
2. Проверьте Network tab
3. Найдите запрос к backend
4. Посмотрите ответ сервера
5. Проверьте статус (должен быть 200)

---

## 💡 Советы для разработки

### 1. Используйте TypeScript
Все компоненты типизированы. IDE покажет ошибки до запуска.

### 2. Проверяйте логи
Все события логируются в консоль в dev режиме.

### 3. Тестируйте сразу
Не накапливайте изменения. Тестируйте каждую фичу отдельно.

### 4. Читайте документацию
Перед добавлением функционала изучите существующий код.

### 5. Следуйте паттернам
Смотрите как реализованы похожие функции и делайте аналогично.

---

## 🎓 Обучающие примеры

### Пример 1: Добавить кнопку с трекингом

```tsx
import { trackPhoneClick } from '@/services/metrika-goals';

function PhoneButton() {
  const handleClick = () => {
    trackPhoneClick('hero_section');
    window.location.href = 'tel:+79781281850';
  };

  return (
    <button onClick={handleClick}>
      Позвонить
    </button>
  );
}
```

### Пример 2: Добавить поле в форму

```tsx
// В ApplicationForm.tsx
<div>
  <label>Email (необязательно)</label>
  <Input
    type="email"
    value={formData.email || ''}
    onChange={(e) => onFormDataChange({ 
      ...formData, 
      email: e.target.value 
    })}
  />
</div>
```

### Пример 3: Фильтровать города

```typescript
const filteredCities = useMemo(() => {
  return crimeaCities.filter(city => {
    // Ваша логика фильтрации
    return city.name.includes(searchQuery);
  });
}, [searchQuery]);
```

---

## ✅ Чеклист для нового разработчика

- [ ] Клонировал репозиторий
- [ ] Установил зависимости
- [ ] Запустил dev сервер
- [ ] Открыл сайт в браузере
- [ ] Проверил консоль на ошибки
- [ ] Протестировал форму заявки
- [ ] Проверил работу поиска
- [ ] Проверил фильтры городов
- [ ] Открыл Яндекс.Метрику
- [ ] Прочитал PROJECT.md
- [ ] Прочитал PUBLIC_API.md
- [ ] Изучил структуру компонентов
- [ ] Готов к работе! 🚀

---

**Время на старт**: ~5 минут  
**Сложность**: 🟢 Легко  
**Требуемые знания**: React, TypeScript (базовые)
